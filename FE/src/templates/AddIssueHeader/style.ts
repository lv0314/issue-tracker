import styled from 'styled-components';

export const AddIssueHeader = styled.h1`
  width: 100%;
  font-size: ${({ theme }) => theme.fontSize.header};
  color: ${({ theme }) => theme.color.titleArchieve};
  margin-top: 32px;
`;
